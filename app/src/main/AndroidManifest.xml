<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.parkseeun.moca_android">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".ui.main.HomeActivity">
        <intent-filter>
            <action android:name="android.intent.action.MAIN"/>
                <action android:name="luminous.ACTION_MULTIPLE_PICK" />

            <category android:name="android.intent.category.LAUNCHER"/>
        </intent-filter>
        </activity>


        <activity android:name=".ui.community.WriteReviewActivity">

        </activity>
        <activity android:name=".ui.follow.FollowActivity">
        </activity>
        <activity android:name=".ui.plus.PlusActivity">
        </activity>
        <activity android:name=".ui.community.ReviewSearchLocationActivity">
        </activity>

    </application>

</manifest>